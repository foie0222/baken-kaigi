# API Base URL
# Amplify環境変数で設定: VITE_API_BASE_URL
VITE_API_BASE_URL=https://your-api-gateway-url.execute-api.ap-northeast-1.amazonaws.com/prod

# API Key（API Gateway認証用）
# Amplify環境変数で設定: VITE_API_KEY
# 取得方法: aws apigateway get-api-key --api-key <API_KEY_ID> --include-value
VITE_API_KEY=your-api-key-here

# ========================================
# Lambda Response Streaming 用設定
# API Gateway の 29秒タイムアウト制限を回避するために使用
# ========================================

# ストリーミング用エンドポイント（Lambda Function URL）
# CDKデプロイ後に出力される AgentCoreStreamingUrl の値を設定
VITE_STREAMING_ENDPOINT=https://xxxxxxxxxx.lambda-url.ap-northeast-1.on.aws/

# AWS リージョン
VITE_AWS_REGION=ap-northeast-1

# ストリーミング API 用アクセスキー（SigV4署名に必要）
# CDKデプロイ後に出力される StreamingAccessKeyId の値を設定
VITE_STREAMING_ACCESS_KEY_ID=AKIAXXXXXXXX

# ストリーミング API 用シークレットアクセスキー（SigV4署名に必要）
# CDKデプロイ後に出力される StreamingSecretAccessKey の値を設定
# ⚠️ 注意: この値はフロントエンドに埋め込まれるため、最小権限のIAMユーザーを使用
VITE_STREAMING_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
