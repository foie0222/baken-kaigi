name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
    branches: [main]

concurrency:
  group: claude-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  claude-review:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Claude Code Review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt: |
            PR #${{ github.event.pull_request.number }} のコードレビューを行ってください。

            まず `gh pr diff ${{ github.event.pull_request.number }}` でPRの差分を取得し、
            変更内容を分析してください。

            ## レビュー観点

            1. **セキュリティ** - 入力検証、認証、認可、IAM最小権限、シークレットのハードコード
            2. **コード品質** - DDD原則、早期リターン、単一責任、マジックナンバー禁止、any型禁止(TS)、具体的例外型(Python)
            3. **パフォーマンス** - N+1問題、不要な再レンダリング、DynamoDBクエリ効率性
            4. **テスト** - 新機能のテスト追加、エッジケースカバレッジ
            5. **エラーハンドリング** - エラー握りつぶし、フォールバック多用の回避

            レビュー完了後、`gh pr review ${{ github.event.pull_request.number }} --comment --body "..."` で
            レビュー結果をPRレビューとして投稿してください。
            問題がある場合はインラインコメントも使ってください。
          claude_args: |
            --max-turns 10
            --allowedTools "Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr review:*),Bash(cat:*),Bash(wc:*),Read,Glob,Grep"
